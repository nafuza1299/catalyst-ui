# Skeleton browser validation

1. Run `npm run dev` and open the app.
2. Render text, circle, and rect skeletons in both themes.
3. Confirm their surface contrast is visible and animation stops when reduced motion is enabled.
